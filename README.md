# program_on_c

## add gitignore

## add hello_world.c 
- 测试浮点数的两种标示方法，点分形式a.b和指数形式aen(aEn)
- 测试浮点数大小，float固定为4字节，double固定为8字节
- 测试整数得到大小，int和Long为4字节，long long为8字结
- 测试数据的内存存储，小端序：低位在低地址。注意：位域无法取地址。 
- 测试字符串的存储，使用数组或指针间接存储。两者的区别是数组可以改变，而指针指向的是字符串常量。
- 测试宽字符，宽字符一般2或4字节存储。"%ls"可用于输出宽字符。
- 测试自增和自减 ,printf函数，好像是等表达式的值计算完毕后才会组成字符串。

## add test_io.c
- 测试格式化输入函数scanf
- 增加密码输入函数，利用getche()函数 
